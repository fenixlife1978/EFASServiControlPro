import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.educontrolpro',
  appName: 'EDUControlPro',
  webDir: 'out',
  server: {
    // Completamente vacío - no debe tener androidScheme ni errorPath
  }
};

export default config;
