apply plugin: 'com.android.application'

android {
    namespace "com.educontrolpro"
    compileSdk rootProject.ext.compileSdkVersion
    
    defaultConfig {
        applicationId "com.educontrolpro"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 6
        versionName "1.5"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        
        aaptOptions {
            ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }
    
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    
    // Importante para Java 8+ (requerido por Firebase y Capacitor)
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

repositories {
    flatDir {
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    
    // --- LIBRERÍAS ADICIONALES PARA EDUCONTROLPRO ---
    implementation "androidx.annotation:annotation:1.7.1" // Para @NonNull y validaciones
    implementation "androidx.lifecycle:lifecycle-service:2.7.0" // Vital para el MonitorService
    
    implementation project(':capacitor-android')
    implementation project(':capacitor-cordova-android-plugins')

    // --- FIREBASE STACK ---
    // Firebase BoM (Bill of Materials) - Centraliza versiones
    implementation platform('com.google.firebase:firebase-bom:33.1.0')
    
    implementation 'com.google.firebase:firebase-database'  // Para logs de búsqueda
    implementation 'com.google.firebase:firebase-firestore' // Para datos de dispositivo
    implementation 'com.google.firebase:firebase-auth'      // Recomendado para seguridad de DB
    implementation 'com.google.firebase:firebase-analytics' // Útil para el Dashboard

    // --- TESTING ---
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
}

apply from: 'capacitor.build.gradle'

// Plugin de Google Services aplicado al final para evitar errores de sincronización
try {
    if (file('google-services.json').exists()) {
        apply plugin: 'com.google.gms.google-services'
    }
} catch(Exception e) {
    logger.info("google-services.json not found, google-services plugin not applied.")
}